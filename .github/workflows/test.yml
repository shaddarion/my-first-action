name: Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 20
      
      - run: npm ci
      
      - uses: ./
        name: Greet Action
        with:
          person: 'Serhii'

      - uses: ./
        name: Greet Action     